"""Module that runs the game, including all top-level gameplay functions and managing things like sprites."""

class Game:
    """A class that runs the game.
    Objects of this class can be saved to allow for game saves
    (e.g., if either player needs to go but doesn't want to forfeit).
    """
    def __init__(self) -> None:
        """A class that runs the game.
        Objects of this class can be saved to allow for game saves
        (e.g., if either player needs to go but doesn't want to forfeit).
        """
        pass

    def play(self) -> None:
        """Play the game."""
        pass
